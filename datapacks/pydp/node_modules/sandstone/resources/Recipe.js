"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RecipeInstance = void 0;
const env_1 = require("../env");
const Resource_1 = require("./Resource");
class RecipeInstance extends Resource_1.ResourceInstance {
    constructor(datapack, name, recipe, options) {
        var _a;
        super(datapack, name);
        /** Give this recipe to the given players. */
        this.give = (targets) => {
            this.datapack.commandsRoot.recipe.give(targets, this.name);
        };
        /** Take this recipe from the given players. */
        this.take = (targets) => {
            this.datapack.commandsRoot.recipe.take(targets, this.name);
        };
        this.recipeJSON = recipe;
        this.datapack.addResource(name, 'recipes', { recipe }, (_a = options === null || options === void 0 ? void 0 : options.onConflict) !== null && _a !== void 0 ? _a : env_1.CONFLICT_STRATEGIES.RECIPE);
    }
}
exports.RecipeInstance = RecipeInstance;
